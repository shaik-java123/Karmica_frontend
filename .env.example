# Rename this file to .env.local for local development
# Or set these in Render dashboard / render.yaml for production

# Backend URL — update to your Render backend URL after deployment
VITE_API_BASE_URL=https://karmika-backend-l5d7.onrender.com/api
